SELECT * FROM hobbies WHERE user_id = ?&quot;, variables: ...) {\n name\n }\n }\n}\n</code></pre>\n<p>I made up the SQL part but I saw something similar anyway. And why not - you can do it. But why? Especially the <code>@rest</code> parts are examples from Apollo Client 2.0. And I am always asking - is it so difficult to write simple resolver with <code>fetch</code> function? GraphQL syntax is very powerful but it doesn&#39;t mean that we must use all the power, right?</p>\n<h2 id=\"last-thoughts\">Last thoughts <a href=\"#last-thoughts\">#</a></h2><p>GraphQL is enabling technology. It enables you to move forward much faster in a cleaner way. A great example is the evolution of GraphQL itself. Did you know that most of the features were developed on client application and not on the server? It all started with FQL that transformed into GraphQL. Then GraphQL fragments - originally implemented only on the server with &quot;defragmentation&quot; and server implemented fragments after ~3 m
SELECT * FROM hobbies WHERE user_id = ?&quot;
