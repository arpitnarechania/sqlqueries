SELECT id, name, name FROM test';
SELECT * FROM testing'));
UPDATE test SET name = ?  WHERE id = ?)
SELECT * FROM test'));
SELECT COUNT(*) FROM test')->fetchColumn());
SELECT id, name, name FROM test' . PHP_EOL;
INSERT INTO interleaved_inserts VALUES (null, %s)
UPDATE test SET name = :name  WHERE id = :id
CREATE TABLE interleaved_inserts ( id INTEGER PRIMARY KEY, name STRING NOT NULL )
CREATE TABLE test ( id INTEGER PRIMARY KEY, name STRING NOT NULL )
CREATE TABLE testing (id INTEGER PRIMARY KEY, name STRING NOT NULL)
