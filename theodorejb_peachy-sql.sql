SELECT * FROM Users WHERE fname LIKE ? AND lname LIKE ?';
SELECT * FROM MyTable
SELECT * FROM Users u INNER JOIN Customers c ON c.CustomerID = u.CustomerID
UPDATE Users SET fname = ?  WHERE user_id = ?
SELECT * FROM Test
SELECT user_id, isDisabled FROM Users WHERE user_id = ?';
SELECT * FROM MyTable a INNER JOIN AnotherTable b ON b.id = a.id
SELECT * FROM nonExistentTable WHERE';
CREATE TABLE Users ( user_id INT PRIMARY KEY AUTO_INCREMENT NOT NULL, name VARCHAR(50) NOT NULL, dob DATE NOT NULL, weight FLOAT NOT NULL, isDisabled BOOLEAN NOT NULL, uuid BINARY(16) NULL )
UPDATE Users SET name = ?  WHERE user_id = ?
CREATE TABLE Test ( name VARCHAR(50) NOT NULL )
CREATE TABLE Users ( user_id INT PRIMARY KEY IDENTITY(1,1) NOT NULL, name VARCHAR(50) NOT NULL, dob DATE NOT NULL, weight FLOAT NOT NULL, isDisabled BIT NOT NULL, uuid BINARY(16) NULL )
SELECT * FROM Users
SELECT * FROM @ids;';
