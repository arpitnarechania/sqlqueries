CREATE TABLE $table (")
select * from " + UserTableMeta.TABLE + " where " + UserTableMeta.COLUMN_ID + "=?
SELECT * FROM tweets JOIN users ON tweets.user_name = users.name WHERE tweets.user_name = ?
select * from " + UserTableMeta.TABLE).build();
select * from who_cares
