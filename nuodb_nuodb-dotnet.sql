select id, number, name, position, team from hockey
select * from sometable
select * from hockey where number = 2
insert into tmp values ('first', 1)
insert into t2 values ('rset 2, row1', 'first', 100)
select field1 from input_data;
create table tmp (strvalue string, numvalue int)
insert into t1 values ('rset 1, row1', 0)
create table timezone (asTimestamp timestamp, asDate date, asTime time, asString string)
select max(id) from hockey
select name from hockey where id = ?
select * from system.connections where connid=GetConnectionID()
update hockey set number = 99  where team = ?
create table temp (col string)
select * from temp
select * from hockey limit 1
update hockey set number = 99  where team = Bruins
select * from hockey where number = @number and team = @team
select col from temp
create table temp (col1 string)
select SourceExpenseId, ExpenseAmount from ExpenseTest
insert into temp values (?)
select count(*) from hockey
insert into temp values (?)
select count(*) from temp
select * from hockey where number = ?.number and team = ?.team
SELECT * FROM dbo.Posts WHERE Author = @author", new SqlParameter("@author
create table temp2 (col1 integer, col2 integer)
create table xyz (col int)
CREATE TABLE Users( PKID String NOT NULL PRIMARY KEY, Username Text (255) NOT NULL, ApplicationName Text (255) NOT NULL, Email Text (128) NOT NULL, Comment Text (255), Password Text (128) NOT NULL, PasswordQuestion Text (255), PasswordAnswer Text (255), IsApproved Boolean, LastActivityDate DateTime, LastLoginDate DateTime, LastPasswordChangedDate DateTime, CreationDate DateTime, IsOnLine Boolean, IsLockedOut Boolean, LastLockedOutDate DateTime, FailedPasswordAttemptCount Integer, FailedPasswordAttemptWindowStart DateTime, FailedPasswordAnswerAttemptCount Integer, FailedPasswordAnswerAttemptWindowStart DateTime)
select * from hockey
select asTimestamp, asDate, asTime, asString from timezone
create table tmp (numvalue1 decimal(15,6), numvalue2 number)
CREATE TABLE {0}.{1} (", SqlGenerator.QuoteIdentifier(schemaName), SqlGenerator.QuoteIdentifier(tableName))
select name from names where id=?
create table temp (col1 bigint, col2 int)
insert into tmp values (?, ?)
select * from tmp
insert into temp values (0, 0)
create table temp (col1 integer, col2 integer)
select position from hockey order by number
SELECT * FROM dbo.Posts WHERE Author = @p0
select * from hockey where number = ?
insert into temp values (input_data)
create table names (id bigint generated always as identity primary key, name string)
