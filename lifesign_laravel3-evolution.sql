CREATE TABLE `sessions` (	 `id` VARCHAR(40) NOT NULL,	 `last_activity` INT(10) NOT NULL,	 `data` TEXT NOT NULL,	 PRIMARY KEY (`id`)	)
SELECT * FROM ($sql) AS TempTable WHERE RowNum {$constraint}
select * from users where name = ?', array('test'));
select email from users where id = 1');
select count(*) from users');
CREATE TABLE sqlite_sequence(name,seq)
insert into users values (?, ?)
CREATE TABLE sessions (sess_id VARCHAR(128) PRIMARY KEY, sess_data TEXT, sess_time INTEGER)
select * from users where id = ?', array($id));
CREATE TABLE sessions (sess_id VARCHAR(128) PRIMARY KEY, sess_data TEXT, sess_time INTEGER)
select * from users where id = 1');
select * from users');
update users set name = ? where id = ?
CREATE TABLE '.$this->wrap($table).' ('.$columns.')
SELECT * FROM sessions')->fetchAll());
