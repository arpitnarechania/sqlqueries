SELECT * FROM frontpage');
UPDATE user SET email = CONCAT WHERE email IS NULL
SELECT * FROM user WHERE user = "'.$item['user'].'";');
SELECT * FROM frontpage ORDER BY sort_order ASC');
SELECT * FROM comment WHERE id = :id
CREATE TABLE IF NOT EXISTS `akismet_log` ( `id` int(11) NOT NULL AUTO_INCREMENT, `comment_id` int(11) NOT NULL, `timestamp` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP, `action` varchar(10) NOT NULL, `is_spam` tinyint(1) NOT NULL, `error` text NULL, `request` text NULL, `response` text NULL, PRIMARY KEY (`id`), KEY `comment_id` (`comment_id`) )
SELECT * FROM comment_like ORDER BY id DESC LIMIT 1
SELECT id FROM archive_issue WHERE issue = '.$file['issue'].' AND publication = '.$file['publication']);
SELECT * FROM text_story WHERE id = '.$notice['text']);
SELECT * FROM comment WHERE external = 0');
UPDATE role SET id = 0  WHERE id = 1
SELECT * FROM category');
