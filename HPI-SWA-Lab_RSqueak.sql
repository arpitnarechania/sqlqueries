insert into players values (', idx, ', 100)
SELECT * FROM MATCHES;
insert into players values (', idx, ', 100)
CREATE TABLE IF NOT EXISTS PLAYERS (playerId integer, rank integer)
CREATE TABLE IF NOT EXISTS %s (id INTEGER)
INSERT INTO MATCHES values (', playerA, ', ', playerB, ', ', winner, ')
INSERT INTO MATCHES values (?, ?, ?)
INSERT INTO PLAYERS values (', idx, ', 100)
INSERT INTO PLAYERS values (?, 100)
SELECT * FROM %s;
CREATE TABLE IF NOT EXISTS MATCHES (playerIdA integer, playerIdB integer, winnerId integer)
UPDATE players SET rank=?  WHERE playerID=?
